# relay-engine — copy to .env (never commit .env)
PORT=8002
DATABASE_URL=postgres://relay:relay@localhost:5432/relay?sslmode=disable

# Rescue / matching tunables
RESCUE_DEFAULT_RADIUS_KM=3
RESCUE_DISCOUNT_BASE=0.15
RESCUE_DISCOUNT_MAX=0.45
PAIR_RESCUE_RADIUS_KM=10
